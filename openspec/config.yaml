schema: spec-driven
context: |
  Tech stack: React 18 (CRA), Tailwind CSS 3, Node.js/Express, MongoDB/Mongoose
  Architecture: Fullstack MERN (web/ frontend, api/ backend), Mobile-First UI
  Testing: Jest (CRA builtin + api jest 29), RTL, mongodb-memory-server
  Style: ESLint (react-app), Tailwind utilities, JavaScript puro (no TS)
  Deploy: Fly.io (Dockerfile, fly.toml)
  Conventions: h-dvh (no h-screen/100vh), min 16px inputs, date-fns for dates
rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    - Load relevant coding skills for the project stack
    tdd: true
    test_command: "cd api && npm test && cd ../web && CI=true npm test"
  verify:
    test_command: "cd api && npm test && cd ../web && CI=true npm test"
    build_command: "cd web && npm run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas (large removals)
